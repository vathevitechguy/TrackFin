import React from 'react';
import styles from '../style';

function Dashboard() {
  return (
    <div className="bg-primary w-[20%] overflow-hidden">
      <div className={`${styles.boxWidth}`}>Dashboard</div>
    </div>
  );
}

export default Dashboard;
